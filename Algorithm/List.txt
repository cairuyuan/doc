单链表

相交
那么交点以后的节点都是公用的，不可能再分开。因为一个节点只有一个next指针

有环，那么环只有入口没有出口。环位于链表的最后。因为一个节点只有一个next指针

两个可能有环的链表是否相交
	链表相交，相交之后又不能分开，还得有环，要么进入环之前已相交，要么从不同的入口点进入环。
	判断方法：
		1 判断是否有环，如果两个都没有，按照没有环的办法处理
		2 若一个有，一个没有，就不会相交
		3 若两个都有，则求其中一个的环上一点，检查这个点是否在另一个链表的环上